using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Player
{
    public class PlayerController : CharacterController
    {
        /*private CapsuleCollider2D _collider2D;
        private Rigidbody2D _rigidbody2D;
    
        [Range(2.0f, 7.0f)] [SerializeField] private float speed = 3.0f;
        [Range(3.0f, 10.0f)] [SerializeField] private float jumpSpeed = 4.0f;
        [Range(0.0f, 85.0f)] [SerializeField] private float slopeLimit = 45.0f;

        [SerializeField] private float minMoveDistance = 0.001f;*/
    
    
    
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}

